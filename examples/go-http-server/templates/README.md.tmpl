# {{ .projectName }}

A minimal Go HTTP server generated by kikplate.

## Run

```bash
go run main.go
```

Server listens on port {{ .port }}.
{{ if .modules.docker.enabled }}
## Docker

```bash
docker build -t {{ .projectName | lower }} .
docker run -p {{ .port }}:{{ .port }} {{ .projectName | lower }}
```
{{ end }}
